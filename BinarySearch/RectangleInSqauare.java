package BinarySearch;

public class RectangleInSqauare {
    static void noforectangles(int x, int a, int b){
        double side1=Math.ceil(x/a);            
        double side2=Math.ceil(x/b);
        double n=side1*side2;
        
        while()
    }

    int f(int x,int a, int b,int n){
        double side1=Math.ceil(x/a);            
        double side2=Math.ceil(x/b);
        if(side1*side2>=n)
            return 1;
        else{
            return 0;
        }
    }

    
    
}
